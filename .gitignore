# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# Local env
.env*.local

# Machine-local npm config (corporate registry/proxy; not for deployment)
.npmrc

# Lockfile bakes in machine-specific (corporate Nexus) resolved URLs; ignore it
package-lock.json

# Server's on-disk analysis store (regenerated by the backend)
.cache/
